package com.gemnav.routing.domain

/**
 * Logical routing tier for GemNav.
 * Later this will be derived from the actual subscription / plan state.
 */
enum class RoutingTier {
    FREE,
    BASIC,
    PRO
}
